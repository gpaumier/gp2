extends master.jade

block article
    article.row
        if collections[archive] && collections[archive].length  > 0
            each item in collections[archive]
                block snippet
                    include _snippet
            else
                h3 No articles to display
